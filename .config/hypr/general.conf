


#displays
monitor = DP-5, 2560x1440@180, 0x0, 1
monitor = DP-3, 2560x1440@180, 2560x0, 1


#reduces lag
render {
direct_scanout = true
} 

general {
    gaps_in = 5
    gaps_out = 15
   border_size = 2
 allow_tearing = true
        layout = dwindle
}


# decor

decoration {
    rounding = 10
    rounding_power = 2
    active_opacity = 0.95
    inactive_opacity = 0.95

   shadow {
        enabled = true
        range = 10
        render_power = 3
        color = rgba(1a1a1aee)
    }

  blur {
  enabled = true	
  size = 5
  passes = 3
  ignore_opacity = true
  new_optimizations = true
  }

}




#animations, mostly default

animations {
    enabled = yes
 bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 1.94, almostLinear, fade
    animation = workspacesIn, 1, 1.21, almostLinear, fade
    animation = workspacesOut, 1, 1.94, almostLinear, fade
}

# workspaces

workspace=1, monitor:DP-5, default:true, persistent:true
workspace=2, monitor:DP-5, default:true, persistent:true
workspace=3, monitor:DP-5, default:true, persistent:true
workspace=4, monitor:DP-5, default:true, persistent:true


# idk what these do


dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

#plugins

### this plugin is broken currently

#plugin:overview:onBottom = true
#plugin:overview:panelHeight = 250
#plugin:overview:autoDrag = true
#plugin:overview:switchOnDrop = true



plugin {
    hyprexpo {
        columns = 1
        gap_size = 5
        bg_col = rgb(111111)
        workspace_method = center current # [center/first] [workspace] e.g. first 1 or center m+1

        gesture_distance = 300 # how far is the "max" for the gesture
    }
}

# peripheral things

input {

accel_profile = flat
force_no_accel = true


  kb_layout = us,no
  kb_options =  grp:alt_space_toggle

}


